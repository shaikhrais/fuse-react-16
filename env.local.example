# Data Provider Configuration
# Set the default data provider: 'mock' or 'light'
NEXT_PUBLIC_DATA_PROVIDER=mock

# LightAPI Configuration (only needed if using 'light' provider)
NEXT_PUBLIC_LIGHT_API_URL=http://localhost:3001

# Other environment variables...
# NEXT_PUBLIC_BASE_URL=https://your-domain.com